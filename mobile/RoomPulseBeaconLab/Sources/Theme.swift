import SwiftUI

/// RoomPulse brand palette. Teal is the single accent across the app, the web
/// and the app icon. This deep teal reads on the app's light surfaces; it's the
/// same brand hue as the web's mint #2FE6B0, shaded for light backgrounds.
enum Brand {
    static let teal = Color(red: 0.0, green: 0.60, blue: 0.46)
}
